# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment (secrets, local overrides)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor / IDE (keep shared .vscode settings in repo if you add them later)
*.swp
*.swo
*~

# Optional local caches (if tooling adds them later)
.cache/
